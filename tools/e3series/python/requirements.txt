# Зависимости инструмента E3.series <-> Excel
#
# openpyxl — чтение и запись .xlsx без установленного Microsoft Excel.
# pywin32  — доступ к COM-интерфейсу E3.series. Только Windows.
#
# Интерфейс собран на tkinter, он входит в состав Python — ставить нечего.
openpyxl>=3.1
pywin32>=306; sys_platform == "win32"

# --- необязательно, но полезно -------------------------------------------------
# Официальная типизированная обёртка Zuken: даёт автодополнение по API E3 и
# показывает в подсказке минимальную версию TLB для каждого метода. Программа
# работает и без неё, но при доработке кода она сильно экономит время.
# Версия подбирается под вашу E3: для 23.20 (2022 SP2) Zuken рекомендует 26.10.0.
#
#   pip install e3series==26.10.0
