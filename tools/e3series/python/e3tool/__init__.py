"""Инструмент обмена данными между E3.series и Excel.

Модули:
    columns   — схема столбцов книги (совместима с HTA-версией)
    util      — разбор чисел, нормализация обозначений, строки таблиц
    e3api     — обёртка над COM-интерфейсом E3.series
    project   — кэш проекта и сопоставление обозначений
    excel_io  — чтение и запись .xlsx через openpyxl (Excel не нужен)
    export    — выгрузка проекта в книгу
    importer  — загрузка книги в проект
    worker    — рабочий поток, единолично владеющий соединением с E3
    ui        — окно на tkinter
    cli       — консольный режим
"""

__all__ = ["__version__"]
__version__ = "1.3.0"
