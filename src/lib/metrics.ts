/**
 * Одна мера на всю оболочку.
 *
 * Раньше размеры назначались там, где рисовались: панель 52, кнопка 36, значок
 * 18, клетка стола 96×100, значок в трее 19. Ни одно из этих чисел не выведено
 * из другого — оттого и ощущение «всё крупное»: элементы были на четверть
 * больше системных, и рядом с настоящим рабочим столом программа выглядела
 * увеличенной лупой.
 *
 * Здесь числа стоят рядом и потому сравнимы. Менять их надо здесь: размер,
 * поставленный «прямо в разметке», через месяц не найдёт глазами никто.
 *
 * Проверяется scripts/test-metrics.ts — он же и не даёт числам вернуться в
 * разметку оболочки.
 */

/** Высота панели задач: кнопка 32 плюс по 4 сверху и снизу */
export const BAR_H = 40;
/** Кнопка на панели задач и в трее — один рост на весь ряд */
export const BAR_BTN = 32;
/** Значок внутри кнопки панели: системный размер мелкого значка */
export const BAR_ICON = 16;
/** Ширина полоски «показать стол» у самого края */
export const BAR_EDGE = 10;

/** Счётчик и чип на кнопке панели: круг, в который влезает «99+» */
export const CHIP_H = 16;
/** Полоска «программа запущена» под кнопкой */
export const RUN_MARK = 2;
/** Меню «Пуск»: ширина и значок плитки */
export const START_W = 520;
export const TILE_BOX = 32;
export const TILE_ICON = 20;

/** Размер значков рабочего стола — выбор человека, как «Вид» в системе */
export type DeskScale = 'small' | 'normal' | 'large';

export interface DeskMetric {
  /** Клетка: значок плюс две строки подписи */
  w: number;
  h: number;
  /** Сам значок */
  icon: number;
  /** Кегль подписи */
  label: number;
}

/**
 * Три размера, как в системе. «Обычные» 76×84 — то, что даёт Windows при 100 %;
 * прежние 96×100 были ближе к «крупным», и три столбца на ноутбуке вместо пяти
 * брались именно отсюда.
 */
export const DESK: Record<DeskScale, DeskMetric> = {
  small: { w: 60, h: 68, icon: 24, label: 10 },
  normal: { w: 76, h: 84, icon: 32, label: 11 },
  large: { w: 96, h: 104, icon: 40, label: 11 },
};

export const DESK_DEFAULT: DeskScale = 'normal';

export const DESK_SCALES: { id: DeskScale; label: string }[] = [
  { id: 'large', label: 'Крупные значки' },
  { id: 'normal', label: 'Обычные значки' },
  { id: 'small', label: 'Мелкие значки' },
];

/** Метрика по названию размера; неизвестное имя — обычные, а не поломка */
export const deskMetric = (scale?: DeskScale | null): DeskMetric =>
  DESK[(scale as DeskScale) in DESK ? (scale as DeskScale) : DESK_DEFAULT];

/** Высота строки списка: плотно и обычно */
export const ROW_H = { dense: 28, normal: 32 };
