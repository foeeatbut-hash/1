/**
 * Лестница слоёв оболочки.
 *
 * Система обещает: Пуск поверх всего, панель задач поверх окон, диалог поверх
 * панели. Обещание держится ровно до тех пор, пока порядок слоёв живёт в одном
 * месте. Пока он был рассыпан по разметке (`z-30` тут, `z-50` там), меню «Пуск»
 * рисовалось внутри панели задач и оказывалось ПОД окнами программ — самое
 * заметное нарушение из всех: перекрытый Пуск сразу читается как «это не
 * система, это программа».
 *
 * Числа с запасом в сотню: между соседями всегда есть куда встать новому слою,
 * не переписывая остальные.
 */
export const Z = {
  /** Стол и значки на нём */
  desktop: 0,
  /** Окна программ: между собой порядок считает windowStore */
  windows: 10,
  /** Панель задач */
  taskbar: 1000,
  /** Всплывающие панели трея: уведомления, помощник, часы, проект */
  tray: 1100,
  /** Пуск — выше всего, что можно открыть мышью */
  start: 1200,
  /** Призрак значка или окна при перетаскивании */
  drag: 1300,
  /** Диалоги программы */
  modal: 1400,
  /** Всплывающие сообщения */
  toast: 1500,
  /** Заставка, вход, лицензия — поверх всего, включая сообщения */
  boot: 1600,
  /**
   * Управление окном самой программы — выше вообще всего, включая заставку.
   * Свернуть и закрыть Flux человек должен мочь в любую секунду, в том числе
   * когда внутри что-то зависло на запуске.
   */
  frame: 1700,
} as const;

export type Layer = keyof typeof Z;
